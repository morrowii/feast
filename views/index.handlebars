
<div class='col-6'>

    {{#each feasts}}

        {{#unless devoured}}

            <h3 class='d-inline'>{{feast_name}}</h3><button class='d-inline'>Devour</button>

        {{/unless}}

    {{/each}}

</div>

<div class='col-6'>

    {{#each feasts}}

        {{#if devoured}}

            <h3 class='d-inline'>{{feast_name}}</h3><h3 class='d-inline'>Devoured</h3>

        {{/if}}

    {{/each}}

</div>